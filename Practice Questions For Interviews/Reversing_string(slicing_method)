#Slicing_method
#NOTE NOTE NOTE: I always get confused and use input.reverse() function, but IT WORK ONLY IN LIST, NOT IN STRING


print("Reversing an string")

string = input("Enter string to be reversed")

new_string = string[::-1]

#here :: means from first to last and -1 means start from last


print(new_string)
